
services:
  # # webmin-only container, TODO eventually merge into webserver container
  # webmin:
  #   container_name: webmin 
  #   restart: unless-stopped
  #   build: ./webmin
  #   # image: webmin:v1
  #   ports:
  #    - '10000:10000'
  #   tty: true

  # # shitty nginx webserver description, from wordpress tutorial
  # webserver:
  #   depends_on:
  #     - wordpress
  #   build: ./caddy
  #   container_name: webserver
  #   restart: unless-stopped
  #   ports:
  #     - "80:80" # http
  #     - "443:443" # https
  #     - '10000:10000' # webmin (TODO add later)
  #   volumes:
  #     - wordpress:/var/www/html # wordpress site contents
  #     - ./caddy/conf:/conf # caddy configuration files
  #   networks:
  #     - app-network


  db:
    image: mysql:8.0
    container_name: db
    restart: unless-stopped
    env_file: .env
    environment:
      - MYSQL_DATABASE=wordpress
      - MYSQL_USER=$MYSQL_USER
      - MYSQL_PASSWORD=$MYSQL_PASSWORD
      - MYSQL_ROOT_PASSWORD=$MYSQL_ROOT_PASSWORD
      - MYSQL_DEFAULT_AUTHENTICATION_PLUGIN=caching_sha2_password
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - app-network

  wordpress:
    depends_on:
      - db
    image: wordpress:php8.2-fpm-alpine
    container_name: wordpress
    restart: unless-stopped
    env_file: .env
    environment:
      - WORDPRESS_DB_SSL=true
      - WORDPRESS_DB_HOST=db:3306
      - WORDPRESS_DB_NAME=wordpress
      - WORDPRESS_DB_USER=$MYSQL_USER
      - WORDPRESS_DB_PASSWORD=$MYSQL_PASSWORD
    volumes:
      - wordpress:/var/www/html
    networks:
      - app-network

  webserver:
    depends_on:
      - wordpress
    image: caddy:latest
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./caddy/conf:/etc/caddy
      - wordpress:/var/www/html # wordpress site contents
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - app-network

volumes:
  certbot-etc:
  wordpress:
  dbdata:
  caddy_data:
  caddy_config:

networks:
  app-network:
    driver: bridge
